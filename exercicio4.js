function dividindoarray(numeros){
   const divisor = []

   const primeiroNumero = numeros[0] /2
   const ultimoNumero = numeros[numeros.length-1] /2

   divisor.push(ultimoNumero)
   divisor.push(primeiroNumero)

   //divisor[0] = ultimoNumero
   //divisor[1] = primeiroNumero

 return divisor


}